# Usage
Open the terminal at where you requirements file is, the run `python -m sort_requirements_file`, the application will auto detect you requirements file, and boom, your requirements file is sorted!
